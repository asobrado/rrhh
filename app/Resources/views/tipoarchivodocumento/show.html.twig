{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tipoarchivodocumento</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipoArchivoDocumento.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipoArchivoDocumento.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipoarchivodocumento_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipoarchivodocumento_edit', { 'id': tipoArchivoDocumento.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
